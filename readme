create it:
python3.6 -m venv venv
activate it:
source venv/bin/activate
install Flask in it:
pip install flask
confirm:
>>> import flask
>>> _
(venv) vmfreebsd11% export FLASK_APP=microblog.py
(venv) vmfreebsd11% export LC_ALL=en_US.UTF-8
(venv) vmfreebsd11% export LANG=en_US.UTF-8

